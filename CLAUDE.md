# CLAUDE.md — nobodybeatstheviz.github.io

The NBTV website — GitHub Pages, `main` branch, CNAME → nobodybeatstheviz.com. This clone (in `Documents\CODING\`, the coding root) is THE local working copy — the wax-system copy was removed 2026-08-25; never work from a second clone.

## Workflow (moved from the wax-system harness, 2026-08-25)

- One update per fresh chat; **read the repo state first** — never work from memory of the site.
- Show the diff before committing; **explicit push gate** — Wax says push, nothing auto-publishes.
- `wax-baseball-dbt/` in this repo is **published output** from the separate `wax-baseball-dbt` repo's GitHub Action (`keep_files: true` is load-bearing) — never hand-edit it here.

## Brand

Canon lives in the wax-system repo: `C:\Users\georg\wax-system\nbtv-brand\` + `C:\Users\georg\wax-system\.claude\rules\nbtv-brand.md` (tagline tiers, The Viz P3 voice — "bits", no byte puns, "on wax" earned-only). Read those before copy or design changes.
